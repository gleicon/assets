{
  "name": "meu-nginx",
  "image": "nginx:latest",
  "ports": {
    "80/tcp": "8080"
  },
  "env": [
    "NGINX_HOST=localhost",
    "NGINX_PORT=80"
  ],
  "volumes": [
    "/host/path:/container/path:ro"
  ],
  "restart": "unless-stopped",
  "networks": ["bridge"]
}